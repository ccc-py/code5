請參考 _doc/下的內容，做出一個使用 NVIDIA 服務的 python 套件，類似 agent.py ，但是要寫成 pypi 套件，要加上詳細的測試
測試的時候，請加入 mock ，讓程式不需要呼叫 LLM 就可以運作。
但是正式運行的時候，可以選擇用 mock 或者 LLM
先規劃，寫在 _doc/v0.1.md 中，我確認後再開始

專案名稱叫 code5 ，最後要做的和 opencode 差不多

https://opencode.ai/
